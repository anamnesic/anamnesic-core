# Kairos Web

TypeScript-only web client for Kairos, built with SolidJS + Vite.

## Development

From the repo root:

```bash
bun install
bun run --cwd apps/web dev
```

## Build

```bash
bun run --cwd apps/web build
```

## Preview

```bash
bun run --cwd apps/web preview
```
